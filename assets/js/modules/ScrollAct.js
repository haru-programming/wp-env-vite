

export const ScrollAct = () => {

	/* get info.
	--------------------------------------------------------------------*/
	//document.documentElement.style.setProperty('--gh', `${document.getElementById('wrapper').getElementsByClassName('sticky-component')[0].clientHeight}px`);

	// const scrollTop = document.documentElement.scrollTop || document.body.scrollTop;

	// if(scrollTop>60){
	// 	if(!document.documentElement.classList.contains('is-scroll')){
	// 		document.documentElement.classList.add('is-scroll');
	// 	}
	// }else{
	// 	if(document.documentElement.classList.contains('is-scroll')){
	// 		document.documentElement.classList.remove('is-scroll');
	// 	}
	// }

}